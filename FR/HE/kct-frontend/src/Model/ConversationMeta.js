/**
 * @type {Object}
 * @property {Boolean} mute To indicate if current conversation is in mute or unmute state
 * @property {Boolean} fullScreen To indicate if user is in full screen or not
 */
const ConversationMeta =  {
    mute: true,
    fullScreen: true,
};

export default ConversationMeta;
